-- A script that creates a table in the current DB

-- Create a table `first_table`
-- with id INT and name VARCHAR(256) attributes

CREATE TABLE IF NOT EXISTS first_table
    (
     id INT,
     name VARCHAR(256)
    );
